---
phase: 01-ingest-deduplication-master-listing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env
  - test/config.test.js
  - test/jaroWinkler.test.js
  - test/atomicWrite.test.js
autonomous: true
requirements: ["INGEST-01", "MERGE-02"]

must_haves:
  truths:
    - "Vitest test runner executes the test suite successfully"
    - "dotenv loads environment variables from local .env correctly"
  artifacts:
    - path: "package.json"
      provides: "dependencies for dotenv and devDependencies for vitest"
      contains: '"vitest"'
    - path: "test/jaroWinkler.test.js"
      provides: "Wave 0 test stubs for Jaro-Winkler company name similarity matching"
    - path: "test/atomicWrite.test.js"
      provides: "Wave 0 test stubs for atomic temp-to-rename file writer"
    - path: "test/config.test.js"
      provides: "environment variable configuration verification tests"
---

<objective>
Configure the development environment with Dotenv and Vitest, and establish the test runner and stub test files for Wave 0.

Purpose: To ensure security configurations are loaded from .env instead of hardcoded strings, and to set up the test runner to satisfy the Nyquist validation strategy.
Output: Configured package.json, local environment bindings, and test suites with initial failing stubs.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingest-deduplication-master-listing/01-CONTEXT.md
@.planning/phases/01-ingest-deduplication-master-listing/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up project config and dotenv configuration</name>
  <files>package.json, package-lock.json, .env, test/config.test.js</files>
  <read_first>package.json, .env</read_first>
  <action>
    Install `dotenv` as a runtime dependency and `vitest` as a devDependency in `package.json` using `npm install dotenv` and `npm install -D vitest`. Add a test script `"test": "vitest run"` to `package.json`. Create a `test/config.test.js` file to verify that the environment variables are loaded properly using `require('dotenv').config()`. Verify `UPSTOX_ACCESS_TOKEN` loads correctly without disclosing it in assertions.
  </action>
  <verify>npx vitest run test/config.test.js</verify>
  <acceptance_criteria>
    - `package.json` contains `"dotenv"` and `"vitest"` devDependency.
    - `package.json` contains `"test": "vitest run"`.
    - `test/config.test.js` contains a test ensuring `process.env.UPSTOX_ACCESS_TOKEN` is populated.
  </acceptance_criteria>
  <done>dotenv is installed and test runner successfully runs test/config.test.js verifying env loader</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Wave 0 test stubs for fuzzy similarity and atomic writer</name>
  <files>test/jaroWinkler.test.js, test/atomicWrite.test.js</files>
  <read_first>.planning/phases/01-ingest-deduplication-master-listing/01-VALIDATION.md</read_first>
  <action>
    Create test suite skeleton files `test/jaroWinkler.test.js` and `test/atomicWrite.test.js` containing initial failing assertions for their respective modules to satisfy Wave 0 compliance. 
    `test/jaroWinkler.test.js` should assert Jaro-Winkler similarity logic (e.g. 'Apex Logistics' vs 'Apex Diagnostics' should fail matching, while similar names with corporate suffix removal should match).
    `test/atomicWrite.test.js` should assert writing to a file atomically via temp file renaming.
  </action>
  <verify>npx vitest run test/jaroWinkler.test.js || true</verify>
  <acceptance_criteria>
    - `test/jaroWinkler.test.js` exists and contains stub assertions.
    - `test/atomicWrite.test.js` exists and contains stub assertions.
  </acceptance_criteria>
  <done>Wave 0 test stubs are present on disk</done>
</task>

</tasks>

<threat_model>
### Trust Boundaries
- Environment Variables boundary: The process boundary between the shell runtime environment and the local filesystem `.env` file containing secrets.
- Third-party packages boundary: Dependency execution via `npm install` and Node.js require.

### STRIDE Threat Register
| Threat ID | Category | Description | Mitigation/Acceptance |
|---|---|---|---|
| T-01-SC | Spoofing / Tampering | Malicious or tampered packages from npm registry (e.g. `dotenv`, `vitest`). | Mitigate: Lock dependencies in `package-lock.json` and use exact dependency versions; inspect package sources. |
| T-01-01 | Information Disclosure | Leaking `UPSTOX_ACCESS_TOKEN` via git repository or runtime debug logs. | Mitigate: Ensure `.env` is listed in `.gitignore` (pre-configured), and explicitly verify in `test/config.test.js` that the token is loaded but NEVER output in logs or printed. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Run `npm install` and ensure all dependencies install without errors.
- [ ] Run `npm test` and verify the test harness starts up and finds files.
- [ ] Verify `.env` is loaded by ensuring `test/config.test.js` passes.
</verification>

<success_criteria>
- All tasks completed.
- Vitest configuration and devDependencies are properly installed.
- Wave 0 test stubs exist under `test/` directory.
</success_criteria>

<artifacts>
### Artifacts this plan produces:
- **File paths:**
  - `test/config.test.js` (Dotenv config verification test suite)
  - `test/jaroWinkler.test.js` (Jaro-Winkler matching test suite stub)
  - `test/atomicWrite.test.js` (Atomic write helper test suite stub)
- **Functions:**
  - None (this is setup / test infrastructure phase)
</artifacts>

<output>
After completion, create `.planning/phases/01-ingest-deduplication-master-listing/01-01-SUMMARY.md`
</output>
